# 角色管理

## RM-01 角色与用户关系

- 角色用于定义用户可见的菜单、可执行的业务操作及可访问的数据范围。
- 一个用户可配置一个或多个角色；用户登录后直接加载全部已分配角色的授权集合，无需选择当前角色。
- 角色变更仅影响后续访问权限，不改写既有业务记录中的操作人、处理结果和审计信息。

## RM-02 角色基础信息

- 新增或编辑角色时，须维护角色名称和数据范围。
- 角色名称应清晰表达业务职责，避免与现有角色重名或使用含义不明确的名称。
- 页面预置系统管理员、线索管理员、顾问、运营、财务、医生、护士、客服共 8 类角色；可在权限配置中维护各角色授权。

## RM-03 数据范围

| 数据范围 | 访问边界 |
| --- | --- |
| 全量 | 可访问系统内全部授权模块的数据。 |
| 负责分院 | 仅可访问所属或被授权分院范围内的数据。 |
| 负责患者 | 仅可访问本人负责、参与服务或被明确授权的患者数据。 |

- 数据范围与菜单、操作权限共同生效；拥有菜单不代表可跨越数据范围访问记录。
- 同一用户拥有多个角色时，可访问数据范围取已分配角色授权范围的并集；服务端仍须进行数据权限校验。

## RM-04 菜单与操作按钮权限

- 权限配置到操作按钮级：先选择可见菜单，再勾选该菜单下允许使用的操作。
- 未授权菜单不应在导航中展示；已授权菜单下未勾选的操作不应向用户展示或允许执行。
- 当前可配置菜单及典型操作包括：

| 菜单 | 可配置操作示例 |
| --- | --- |
| 线索管理 | 查看、新建线索、处理重复线索、分配顾问、终止跟进 |
| 回访管理 | 查看、录入结果、改期、查看回访记录、规则配置 |
| 客户管理 | 查看、患者详情、录入沟通记录、分配医护 |
| 合同管理 | 查看、合同签约、查看合同 |
| 合同财务 | 查看、收款登记、催缴 |
| 基础设置 | 分院管理、用户管理、角色管理、字典管理 |

## RM-05 多角色授权合并

- 菜单可见性、操作按钮权限和数据范围均按用户全部角色的授权并集计算。
- 任一角色已授予的菜单或操作，即对该用户生效；移除该角色后，只有该角色独有的授权失效。
- 多角色授权合并用于支持兼岗；业务页面仍需按具体操作和数据范围控制，不能仅依赖前端隐藏按钮。

## RM-06 角色职责边界

- 系统管理员负责基础设置、用户管理及角色权限维护。
- 线索管理员负责所属分院的线索核对、顾问分配及公共池处理。
- 顾问负责签约前线索转化、回访结果录入及改期处理。
- 运营负责经营报表与运营指标监控；财务负责合同、收款与催缴协同。
- 医生、护士、客服按“负责患者”范围参与病历、诊疗协同、陪诊或服务沟通，不因角色获得无关患者数据的访问权。

## RM-07 变更与审计

- 新增角色、编辑角色名称或数据范围、调整菜单及操作权限，均应记录操作人、操作时间、变更前后值和执行结果。
- 权限调整生效后，应即时影响用户的新访问与新操作；已完成的历史业务记录保持原样。
- 高风险授权（如角色管理、用户管理、合同财务）应遵循最小权限原则，并保留完整审计记录。
