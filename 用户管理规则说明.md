# 用户管理规则说明

## UM-01 手机号（登录账号）

- 手机号是用户的唯一登录账号，须为中国大陆 11 位手机号。
- 一个手机号只能绑定一个用户；用户管理及关联人员列表完整展示手机号。
- 创建或修改用户时，系统校验手机号格式和唯一性。

## UM-02 短信验证码登录

- 用户在登录页输入手机号后，通过短信验证码登录；系统不设置、保存或重置登录密码。
- 验证码为 6 位数字，5 分钟内有效且只能使用一次。
- 发送频率、失败次数和异常登录处理应遵循平台安全策略，并保留发送与校验审计记录。

## UM-03 账号状态

- 只有“启用”状态的账号可以发送和使用登录验证码。
- 停用立即禁止新的登录会话；历史业务记录、操作人和审计记录不删除、不改写。

## UM-04 角色分配与登录

- 一个用户可配置一个或多个角色。
- 认证成功后直接进入系统，不展示也不要求用户选择角色。
- 用户工作台、菜单和待办按合并后的授权呈现。

## UM-05 权限集合

- 菜单可见性、操作按钮和数据范围取用户所有已分配角色授权的并集。
- 任一角色授予的权限即对该用户生效；角色被移除后，对应权限即时失效。
- 涉及数据范围时，用户可访问任一角色数据范围内的数据；具体数据权限仍须经过后端校验，不仅依赖前端菜单控制。

## UM-06 审计要求

- 新增、编辑、停用/启用用户，调整手机号或角色，以及发送验证码，均须记录操作人、操作时间、变更前后值和执行结果。
- 手机号等个人信息按最小可见原则展示和导出。
