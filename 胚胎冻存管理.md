# 胚胎冻存管理规则说明

## 1. 页面定位

胚胎冻存管理用于统一管理患者冷冻胚胎的保存有效期、到期提醒、续费意向、缴费入账、实验室确认与终止保存流程。页面面向顾问、财务、实验室人员、区域负责人及管理员。

页面解决的核心问题：

- 什么时候到期；
- 患者是否续费；
- 续费多久、应缴多少；
- 是否已经付款并由财务确认入账；
- 实验室是否已确认续存并更新新的保存有效期；
- 患者明确不续费时，是否已完成合规终止流程。

## 2. 管理对象与唯一标识

1. 一条冻存档案对应一次独立冻存批次，使用“冻存档案 ID”作为全局唯一标识；列表中“冻存档案 ID”、女方姓名和男方姓名必须分列展示。
2. 同一对夫妻可以有多个冻存批次，分别维护冷冻日期、胚胎数量、管数和保存有效期。
3. 冻存档案须关联客户档案 ID；有病历号时同步展示，尚未在互创建档时病历号可显示为“-”。
4. 冻存档案与夫妻客户、合同、周期是关联关系，但不能用合同编号或周期号替代冻存档案 ID。

## 3. 数据来源与主数据边界（ES-01）

1. 胚胎数量、冻存管数、冷冻日期、当前保存有效期及实验室确认结果，以实验室系统或 HIS/互创同步结果为准。
2. CRM 负责提醒、协同、意向、联系和历史留痕，不得自行推算或覆盖实验室主数据。
3. “套餐免费保存截止”表示合同权益中免费保存费用的截止日；“当前保存有效期”表示实验室确认的当前保存截止日。两者必须分列展示，不得混用。
4. 合同是否包含首年、半年或其他时长，以患者实际签署的合同版本为准，系统不设置全机构统一默认期限。
5. 保存年限上限、宽限期和收费标准均按机构、实验室及合同版本配置，不在页面写死。

## 4. 核心字段

| 字段 | 规则 |
| --- | --- |
| 冻存档案 ID | 系统生成，唯一且不可修改 |
| 女方姓名 / 身份证号 | 分列展示女方姓名，姓名下展示身份证号 |
| 男方姓名 / 身份证号 | 分列展示男方姓名，姓名下展示身份证号 |
| 病历号 | 互创建档后同步；未建档显示“-” |
| 胚胎数量 / 管数 | 来自实验室/HIS，只读展示 |
| 冷冻日期 | 实际冷冻业务日期 |
| 套餐免费保存截止 | 合同权益中免费保存费用覆盖的截止日 |
| 当前保存有效期 | 实验室确认的当前有效期，也是提醒计算基准 |
| 实验室保存有效期 | 从互创同步的实际有效期；续费确认后在同步前可与当前保存有效期不同 |
| 剩余天数 | 当前保存有效期减当前日期；负数显示逾期天数 |
| 保存状态 | 保存有效、60天内到期、30天内到期、7天内到期、逾期、已续费、终止保存 |
| 续费意向 | 暂未决定、续费6个月、续费1年、续费2年、明确不续费；不提供“待确认”选项 |

## 5. 到期提醒规则（ES-02）

1. 以“当前保存有效期”为唯一提醒计算基准。
2. 到期前 60 天（剩余 31—60 天）：生成“60天到期名单”，提醒对象为对应周期护士。
3. 到期前 30 天（剩余 8—30 天）：生成“30天到期名单”，提醒对象为对应客服；客服必须记录患者当前意向和下次跟进日期。
4. 到期前 7 天（剩余 0—7 天）：生成“7天到期名单”，提醒对象为对应顾问；仍未明确意向时每日提醒。
5. 到期日：生成当日到期提醒，记录是否已付款、是否处于宽限期及下一责任人。
6. 到期后：生成“逾期名单”并提醒对应顾问，显示逾期天数，按机构宽限政策升级处理。
7. 仅未联系、未缴费或到期，不得自动判定患者放弃胚胎或自动发起销毁。

### 5.1 台账排序与筛选

1. 默认按保存状态排序：保存有效、60天内到期、30天内到期、7天内到期、逾期、已续费、终止保存。
2. 每种保存状态在原型台账中各展示一条样例数据。
3. 筛选支持冻存档案 ID、任一方姓名、任一方身份证号和病历号；不展示、也不以客户档案 ID 作为筛选条件。
4. 台账不展示缴费状态和实验室状态列；相关信息仍可在续存协同与历史留痕中查询。
5. 列表操作包括：录入意向、沟通记录、确认续费、续费记录、确认终止；不提供“联系记录”“发起续费”“发起终止”按钮。
6. 保存有效、已续费、终止保存三种状态不展示“确认续费”“确认终止”；60/30/7 天内到期及逾期状态均同时展示两个确认按钮。保存有效状态不展示“续费记录”。

### 5.2 消息名单提醒

1. 系统每天生成“胚胎冷冻60天到期名单”“胚胎冷冻30天到期名单”“胚胎冷冻7天到期名单”和“胚胎冷冻逾期名单”，在顶部消息提醒中按名单聚合推送。
2. 60天到期名单包含剩余 31—60 天的档案并推送给周期护士；30天到期名单包含剩余 8—30 天的档案并推送给客服；7天到期名单包含剩余 0—7 天的档案并推送给顾问；逾期名单包含剩余天数小于 0 的档案并推送给顾问。
3. 名单弹窗必须以冻存档案 ID 作为首列唯一标识，并列出：女方姓名及身份证号、男方姓名及身份证号、病历号、胚胎冷冻日、截止时间、剩余几天到期或逾期几天、冷冻胚胎枚数、管数和提醒对象。
4. 同一患者存在多个冻存批次时，按冻存档案 ID 分别进入名单，不得合并到一行。
5. 名单提醒标记已读只影响消息状态，不关闭顾问跟进任务，也不改变冻存档案状态。

## 6. 续费闭环（ES-03）

续费依次经过以下步骤：

1. 顾问联系患者并记录续费意向、续存期限和沟通备注；
2. 顾问发起续费，系统生成应缴金额和缴费通知；
3. 患者付款后，状态变为“已支付待入账”；
4. 财务核对付款人、金额和渠道流水，确认入账；
5. 入账后自动生成实验室待办；
6. 实验室确认续存，填写确认单号并更新新的当前保存有效期；
7. 只有完成第 6 步，保存状态才可更新为“保存有效/续存完成”。

顾问口头确认、患者付款截图或财务登记收款均不能单独视为续费完成。

## 7. 不续费与终止保存（ES-04）

1. “明确不续费”只表示患者意向，不等于实验室立即终止保存。
2. 发起终止保存前，必须取得符合机构要求的患者明确授权或知情文件。
3. 终止流程至少包括：记录意向、核验身份与授权、上传凭证、内部审批、实验室确认、结果回写。
4. 任何终止、转运、处置操作均应由具备权限的实验室人员执行；CRM 只发起和记录流程。
5. 患者暂未决定、失联、欠费或超过提醒日期时，不得直接改变为“已终止”。

## 8. 历史与审计（ES-05）

以下记录必须按时间追加保存，不得覆盖：

- 顾问每次联系时间、方式、结果、内容及下次跟进日期；
- 患者历次续费意向及变更原因；
- 续存期限、应缴金额、付款与财务入账记录；
- 实验室确认单号、确认人和新旧有效期；
- 终止保存申请、审批、凭证和实验室执行结果；
- 来源系统、同步时间、操作人和操作时间。

## 9. 角色权限

| 角色 | 数据范围 | 主要权限 |
| --- | --- | --- |
| 顾问 | 负责患者 | 查看冻存信息、记录联系与意向、发起续费、发起终止申请 |
| 区域负责人 | 负责区域 | 查看临期与逾期、督办顾问、审批或升级异常 |
| 财务 | 授权分院或全量 | 查看续费账单、确认入账、处理退款及对账异常 |
| 实验室人员 | 负责实验室 | 查看入账结果、确认续存、更新有效期、执行审批通过的终止流程 |
| 管理员 | 全量 | 配置提醒、宽限期、收费标准与权限，查看完整审计 |

敏感操作采用最小权限原则，顾问不得直接修改胚胎数量、管数、冷冻日期和实验室有效期。

## 10. 页面操作规则

1. “录入意向”首项为联系方式，随后记录五种续费意向之一、下次跟进日期和沟通备注。
2. “沟通记录”按时间倒序展示完整沟通内容、联系方式、意向和下次跟进日期；默认展示最近 3 次记录。
3. “确认续费”弹窗展示只读的原截止日期与续费后截止日期；选择续费期限和续费日期后，系统自动计算续费后截止日期并在确认后写入当前保存有效期。实验室保存有效期仍以互创同步数据为准。
4. “续费记录”按时间倒序展示全部既往续费记录。
5. “确认终止”仅在患者明确不续费时可用；确认后保存状态更新为“终止保存”。
6. 列表及当前筛选结果支持导出 Excel，导出内容遵循当前用户数据权限。

## 11. 验收标准

1. 能正确区分套餐免费保存截止和当前保存有效期。
2. 60、30、7 天及逾期提醒可以按配置生成并升级；60天、30天、7天到期名单及逾期名单字段完整、数据准确，并分别推送给周期护士、客服、顾问和顾问。
3. 患者意向、付款、财务入账和实验室确认状态可独立展示。
4. 未完成实验室确认时，不得显示续存完成。
5. 明确不续费后只能发起终止审批，不能直接终止保存。
6. 同一患者多个冻存批次可分别管理并保留独立历史。
7. 所有关键变更均可追溯操作人、时间、前后值和凭证。
